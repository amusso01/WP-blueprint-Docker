#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
# shellcheck disable=SC1091
source "${ROOT}/scripts/_common.sh"

run_args=(run --rm)
if [[ ! -t 0 ]] || [[ ! -t 1 ]]; then
  run_args+=(-T)
fi

exec docker compose "${run_args[@]}" wpcli "$@"
